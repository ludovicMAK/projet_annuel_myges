apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: frontend-secrets
  namespace: frontend
spec:
  authentication:
    serviceToken:
      serviceTokenSecretReference:
        secretName: frontend-infisical-auth
        secretNamespace: frontend
      secretsScope:
        envSlug: "prod"
        secretsPath: "/"
  managedSecretReference:
    secretName: frontend-generated-secrets
    secretNamespace: frontend