apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: frontend
  namespace: frontend
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`myges.local`)
      kind: Rule
      services:
        - name: frontend
          port: 3000