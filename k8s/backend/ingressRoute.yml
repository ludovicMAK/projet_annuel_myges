apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: backend
  namespace: backend
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`api.myges.local`)
      kind: Rule
      services:
        - name: backend
          port: 3001